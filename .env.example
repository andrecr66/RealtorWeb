# Flask Environment Variables
FLASK_APP=run.py
FLASK_ENV=development # Options: development, production

# Secret Key - IMPORTANT: Change this to a strong, random value for production!
# You can generate one using: python -c "import secrets; print(secrets.token_hex(24))"
SECRET_KEY=your_very_secret_random_key_here_please_change_me

# Database Configuration (PostgreSQL)
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=maria_elena_realty

# Optional: Full SQLAlchemy Database URI (if you prefer to set it directly)
# If this is uncommented, it will typically override the individual DB_* variables in config.py
# SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://your_db_username:your_db_password@localhost:5432/maria_elena_realty

# Other application-specific configurations can be added here if needed
# Example:
# ITEMS_PER_PAGE=10
